#include <stdio.h>

/**
 * main - This program prints the alphabet in lowercase
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char j;

	for (j = 'a'; j <= 'z'; j++)
		putchar(j);
	putchar('\n');
	return (0);
}
